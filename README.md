# Jake Amaral — Portfolio

Personal portfolio site. Single self-contained `index.html` (all styles, scripts and images are inline), plus the resume PDF.

Deployed on Vercel as a static site — no build step, no dependencies.
